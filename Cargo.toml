[package]
name = "heap-oracle"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[features]
default = ["ui"]
ui = ["dep:eframe"]

[dependencies]
eframe = { version = "0.29", optional = true }

[build-dependencies]
cmake = "0.1"
